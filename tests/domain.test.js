import { describe,it,expect } from 'vitest';
import { ClaimsEngine, canonicalize, commitment, payout } from '../src/domain.js';
import { demoPolicy } from '../src/fixtures.js';
const caller=demoPolicy.holder;
const claim=(o={})=>({claimId:'clm_test',policyId:demoPolicy.policyId,claimant:caller,validator:demoPolicy.validator,incidentAt:'2026-08-10T12:00:00.000Z',incidentClass:'MISSED_EXECUTION_WINDOW',documentedLoss:600n,finality:'FINAL',explanation:'Bounded claimant explanation.',evidence:[{kind:'DEMO FIXTURE',mode:'fixture',source:'test',finality:'FINAL',usedInAdjudication:false}],...o});
const verdict=(o={})=>({eligibility:'APPROVED',incidentClass:'MISSED_EXECUTION_WINDOW',slashFinal:true,coveredEvent:true,exclusionTriggered:false,eligibleLoss:600n,confidence:.8,evidenceFindings:['supported'],policyFindings:['covered'],reasoningSummary:'Supported in controlled test.',...o});
describe('Slaiv deterministic reference engine',()=>{it('canonicalizes equivalent structures identically',async()=>{expect(canonicalize({b:1,a:'x'})).toBe(canonicalize({a:'x',b:1}));expect(await commitment({b:1,a:'x'})).toBe(await commitment({a:'x',b:1}))});it('creates immutable policy and rejects duplicate/wrong holder',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);expect(()=>e.createPolicy(demoPolicy,caller)).toThrow('Duplicate');expect(()=>new ClaimsEngine().createPolicy(demoPolicy,'0x2222222222222222222222222222222222222222')).toThrow('Unauthorized')});it('prevents wrong claimant and duplicate claims',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);expect(()=>e.submitClaim(claim(), '0x2222222222222222222222222222222222222222')).toThrow('Unauthorized');e.submitClaim(claim(),caller);expect(()=>e.submitClaim(claim(),caller)).toThrow('Duplicate')});it('awaits finality and rejects premature review',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);let c=e.submitClaim(claim({finality:'PENDING'}),caller);expect(c.state).toBe('AWAITING_FINALITY');expect(()=>e.review(c.claimId,verdict())).toThrow('not ready')});it('reviews, caps payout and prevents double finalization',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);e.submitClaim(claim(),caller);e.review('clm_test',verdict());let c=e.finalize('clm_test',caller);expect(c.instruction).toBe(500n);expect(()=>e.finalize('clm_test',caller)).toThrow('cannot be finalized')});it('rejects loss above documented and unauthorized evidence',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);e.submitClaim(claim(),caller);expect(()=>e.appendEvidence('clm_test',{kind:'DEMO FIXTURE',mode:'fixture',source:'x',finality:'FINAL',usedInAdjudication:false},'0x2222222222222222222222222222222222222222')).toThrow('Unauthorized');expect(()=>e.review('clm_test',verdict({eligibleLoss:601n}))).toThrow('exceeds')});it('requires material authorized appeal',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);e.submitClaim(claim(),caller);e.review('clm_test',verdict({eligibility:'DENIED',eligibleLoss:0n}));expect(()=>e.appeal('clm_test','short',{},caller)).toThrow('Invalid');expect(e.appeal('clm_test','New material evidence changes source reconciliation.',{},caller).state).toBe('APPEALED')});it('uses integer arithmetic',()=>{expect(payout(601n,500n,500).payout).toBe(500n);expect(payout(420n,500n,500).payout).toBe(399n)})});
