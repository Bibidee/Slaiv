import { describe,it,expect } from 'vitest';
import { ClaimSchema, PolicySchema, ClaimsEngine } from '../src/domain.js';
import { demoPolicy } from '../src/fixtures.js';
const caller=demoPolicy.holder;
const claim={claimId:'clm_schema',policyId:demoPolicy.policyId,claimant:caller,validator:demoPolicy.validator,incidentAt:'2026-08-10T12:00:00.000Z',incidentClass:'MISSED_EXECUTION_WINDOW',documentedLoss:1n,finality:'FINAL',explanation:'safe',evidence:[{kind:'DEMO FIXTURE',mode:'fixture',source:'test',finality:'FINAL',usedInAdjudication:false}]};
describe('schema and transition safety',()=>{it('rejects invalid policy date bounds',()=>expect(()=>PolicySchema.parse({...demoPolicy,coverageStart:demoPolicy.coverageEnd})).toThrow());it('rejects unsafe evidence URL scheme',()=>expect(()=>ClaimSchema.parse({...claim,evidence:[{...claim.evidence[0],url:'javascript:alert(1)'}]})).toThrow());it('rejects impossible transitions and unauthorized finalization',()=>{let e=new ClaimsEngine();e.createPolicy(demoPolicy,caller);e.submitClaim(claim,caller);expect(()=>e.transition(claim.claimId,'FINAL')).toThrow('Invalid');expect(()=>e.finalize(claim.claimId,'0x2222222222222222222222222222222222222222')).toThrow('Unauthorized')})});
