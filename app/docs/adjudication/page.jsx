import { PageHead } from '../../components';
export default function Adjudication(){return <main className="app-page doc-page"><PageHead eyebrow="ADJUDICATION STANDARD" title="How Slaiv reaches a ruling"><p>Anyone can trigger protocol work. No transaction sender can choose the outcome.</p></PageHead>{[['01','Consensus-verified protocol facts','A claimant assertion never establishes finality. Any wallet may submit a candidate official GenLayer event, but leader and validators independently verify the source before the contract creates a protocol fact.'],['02','Immutable policy terms','Coverage network, dates, validator binding, event classes, exclusions, limits, and deductibles are checked deterministically.'],['03','Permissionless GenLayer judgment','Once finality is verified, any connected wallet may trigger judgment. The caller supplies only the claim ID; GenLayer consensus derives the settlement-critical verdict from stored policy and evidence.'],['04','Protected permissionless settlement','Appeal initiation belongs to the claimant. Appeal judgment is permissionless, and finalization becomes permissionless without allowing a third party to erase an active appeal right. The payout itself is deterministic.']].map(([n,h,p])=><section className="clause" key={n}><code>{n}</code><div><h2>{h}</h2><p>{p}</p></div></section>)}</main>}
