import { PageHead } from '../../components';
export default function Adjudication(){return <main className="app-page doc-page"><PageHead eyebrow="ADJUDICATION STANDARD" title="How Slaiv reaches a ruling"><p>What a coverage holder can expect from the record, the policy, and GenLayer.</p></PageHead>{[['01','Native evidence','A claimant assertion never establishes finality. A normalized protocol fact must be recorded by the separated authority before review can begin.'],['02','Immutable policy terms','Coverage dates, validator binding, event classes, limits, and deductibles are checked deterministically.'],['03','GenLayer judgment','When evidence needs semantic reading, validators assess a structured record and must agree on settlement-critical fields.'],['04','Deterministic settlement','The effective ruling is bounded by documented loss and policy terms. Finalization records the resulting payout instruction.']].map(([n,h,p])=><section className="clause" key={n}><code>{n}</code><div><h2>{h}</h2><p>{p}</p></div></section>)}</main>}
