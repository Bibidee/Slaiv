'use client';
const copy={SIGNING:'Confirm this transaction in your injected wallet.',FINALIZING:'Signed. Waiting for GenLayer consensus and finality.',VERIFYING:'Finalized. Verifying GenVM execution result.',CONFIRMED:'GenVM execution verified. Contract state refreshed.'};
export function TxStatus({tx}){if(!tx?.stage)return null;const failed=tx.stage==='ERROR',title=failed?(tx.hash?'Transaction failed after submission':'Transaction request failed before signing'):copy[tx.stage];return <div className={`tx-status ${failed?'error':''}`} role="status"><b>{title}</b>{tx.hash?<code>{tx.hash}</code>:null}{tx.error?<p>{tx.error}</p>:null}</div>}
